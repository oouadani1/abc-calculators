# ABC Calculators

Commuter benefit calculators.

## Tools

- `mbta-fare-calculator/`
- `bluebikes-membership-calculator/`

## Local preview

Open a tool's `index.html` in a browser, or serve the repo root with a static file server (for example `python3 -m http.server`).

## Build

```
./build.sh
```
